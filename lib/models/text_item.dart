import 'package:flutter/material.dart';

class TextItem {
  final String text;
  Offset position;
  final Color color;
  final double size;

  TextItem(this.text, this.position, this.color, this.size);
}
